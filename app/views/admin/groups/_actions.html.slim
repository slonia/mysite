.btn-group.btn-group-sm.btn-group-vertical
  = link_to t('show'), admin_group_path(group), class: 'btn btn-default'
  = link_to t('edit'), edit_admin_group_path(group), class: 'btn btn-default'
  = link_to t('destroy'), admin_group_path(group), method: :delete, confirm: 'Are you sure?', class: 'btn btn-danger'
