.btn-group.btn-group-sm.btn-group-vertical
  = link_to t('edit'), edit_admin_room_path(room), class: 'btn btn-default'
  = link_to t('destroy'), admin_room_path(room), method: :delete, confirm: 'Are you sure?', class: 'btn btn-danger'
