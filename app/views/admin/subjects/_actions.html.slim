.btn-group.btn-group-sm
  = link_to 'Show', admin_subject_path(subject), class: 'btn btn-default'
  = link_to 'Edit', edit_admin_subject_path(subject), class: 'btn btn-default'
  = link_to 'Destroy', admin_subject_path(subject), data: {confirm: 'Are you sure?'}, method: :delete, class: 'btn btn-danger'
